RewriteEngine On
RewriteBase /

# Pass through existing files and directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route clean slug URLs to index.php
RewriteRule ^([a-z0-9-]+)/?$ index.php?p=$1 [L,QSA]
